apiVersion: v1
kind: Service
metadata:
  name: app1-service
  namespace: preprod
spec:
  selector:
    app: app1
  ports:
    - protocol: TCP
      port: 80        # puerto expuesto del servicio
      targetPort: 8000 # puerto del contenedor FastAPI
  type: NodePort